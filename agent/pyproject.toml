[project]
name = "perspectify-agent"
version = "0.1.0"
description = "Agno AgentOS service for Perspectify research briefs"
requires-python = ">=3.11"
dependencies = [
  "agno>=2.0.0",
  "fastapi[standard]>=0.115.0",
  "httpx>=0.27.0",
  "openai>=1.0.0",
]

[tool.uv]
package = false
